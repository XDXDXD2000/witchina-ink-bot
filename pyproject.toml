[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "witchina-ink-bot"
version = "1.0.0"
dependencies = [
    "aiogram==3.3.0",
    "aiosqlite==0.19.0",
    "python-dotenv==1.0.0",
]